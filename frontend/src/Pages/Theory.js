import React from "react";
import styled from "styled-components";

export default function Theory() {
	return <TheoryStyle>
		this is theory
	</TheoryStyle>;
}

const TheoryStyle = styled.div`
	padding-top: var(--page-top-offset);
`;